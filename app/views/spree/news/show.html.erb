<div data-hook="news">
  <div>
    <h6>
      <%= l(@news_item.created_at, format: "%d %B %Y") %>
    </h6>
    <h1>
      <%= @news_item.title %>
    </h1>
    <%= @news_item.description.html_safe %>
  </div>
</div>
